const TransactionFeePercentage = {
  // Bronze Accounts
  BB: 0.05,
  Bronze: 0.05,

  // Silver Accounts
  BS: 0.03,
  Silver: 0.03,

  // Gold Accounts
  Gold: 0.01,
  BG: 0.01,
};

module.exports = TransactionFeePercentage;
